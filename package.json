{
  "name": "keyboard-navigation-extension",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "watch": "tsc --watch",
    "web-ext": "web-ext run",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^3.5.2",
    "web-ext": "^3.0.0"
  }
}
